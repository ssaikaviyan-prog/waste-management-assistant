# Backend Organization

The live backend remains in `server/` because the Express runtime, tRPC router, and managed WebDev build depend on that path. This folder is reserved for submission notes or future non-runtime organization artifacts.
