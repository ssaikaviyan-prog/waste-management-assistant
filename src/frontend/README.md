# Frontend Organization

The live frontend remains in `client/src/` because the managed Vite configuration and import aliases depend on that path. This folder is reserved for submission diagrams, frontend extracts, or future non-runtime organization artifacts.
