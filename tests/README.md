# Tests

The executable tests remain in `server/` because the project test runner discovers them there. This folder stores submission-oriented test cases, results, screenshots, and the final test report.
