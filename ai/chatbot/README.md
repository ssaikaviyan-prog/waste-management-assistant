# Chatbot Documentation

Store chatbot behavior notes, supported waste-management topics, conversation examples, and future evaluation results here. The active UI is in `client/src/pages/Assistant.tsx` and the server procedure is in `server/routers.ts`.
